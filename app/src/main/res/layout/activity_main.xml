<?xml version="1.0" encoding="utf-8"?>
<!-- Layout principal. Fem servir ConstraintLayout per col·locar les coses lliurement. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Aquest contenidor és on es carregarà el fragment de la llista -->
    <!-- Li diem que ocupi tota la pantalla -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container_llista"
        android:name="com.andrei.bibliotecamixta.ui.LlistaFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- El botó flotant per obrir el menú, situat a baix a la dreta -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_menu"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:src="@android:drawable/ic_menu_sort_by_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="@string/obrir_menu" />

</androidx.constraintlayout.widget.ConstraintLayout>
